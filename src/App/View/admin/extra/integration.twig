{% extends 'admin/layout.twig' %}

{% block title %}Integrace | Administrace{% endblock %}

{% block content %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <p class="text-muted mb-1">Administrace &raquo; Extra</p>
            <h1 class="h3 mb-0">Integrace</h1>
            <p class="text-muted">Nastavte ochranu formulářů pomocí Google reCAPTCHA a hlášení do AbuseIPDB.</p>
        </div>
    </div>

    <form method="post" class="card p-4 shadow-sm">
        <div class="mb-4">
            <h2 class="h5">Google reCAPTCHA</h2>
            <p class="text-muted small">Použijte klíče pro reCAPTCHA v3. Token se bude přidávat ke všem veřejným POST formulářům.</p>
            <div class="form-check form-switch mb-3">
                <input class="form-check-input" type="checkbox" id="recaptcha_enabled" name="recaptcha_enabled" {% if values.recaptcha_enabled %}checked{% endif %}>
                <label class="form-check-label" for="recaptcha_enabled">Zapnout ochranu reCAPTCHA</label>
            </div>
            <div class="row g-3">
                <div class="col-md-6">
                    <label for="recaptcha_site_key" class="form-label">Site key</label>
                    <input type="text" class="form-control" id="recaptcha_site_key" name="recaptcha_site_key" value="{{ values.recaptcha_site_key }}" placeholder="6Lc...">
                </div>
                <div class="col-md-6">
                    <label for="recaptcha_secret_key" class="form-label">Secret key</label>
                    <input type="text" class="form-control" id="recaptcha_secret_key" name="recaptcha_secret_key" value="{{ values.recaptcha_secret_key }}" placeholder="6Lc...">
                </div>
            </div>
            <div class="form-text">Pokud je reCAPTCHA zapnutá, bez platného tokenu nebude možné odeslat veřejné formuláře.</div>
        </div>

        <hr>

        <div class="mb-4">
            <h2 class="h5">AbuseIPDB</h2>
            <p class="text-muted small">Volitelně odešlete hlášení na <a href="https://www.abuseipdb.com/" target="_blank" rel="noopener">AbuseIPDB</a> při neúspěšných bezpečnostních kontrolách.</p>
            <div class="mb-3">
                <label for="abuseipdb_api_key" class="form-label">API klíč</label>
                <input type="text" class="form-control" id="abuseipdb_api_key" name="abuseipdb_api_key" value="{{ values.abuseipdb_api_key }}" placeholder="vložte API klíč">
                <div class="form-text">Klíč je uložen pouze v databázi nastavení.</div>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="checkbox" id="abuseipdb_report_recaptcha" name="abuseipdb_report_recaptcha" {% if values.abuseipdb_report_recaptcha %}checked{% endif %}>
                <label class="form-check-label" for="abuseipdb_report_recaptcha">Posílat hlášení při selhání reCAPTCHA</label>
            </div>
        </div>

        <div class="d-flex justify-content-end">
            <button type="submit" class="btn btn-primary">Uložit integrace</button>
        </div>
    </form>
{% endblock %}
